# Copy to `.env.local` for local dev — never commit real keys
# Values from `supabase status` after `supabase start`

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key

# API Worker (Task 2.13)
VITE_API_URL=http://127.0.0.1:8787
# Staging (Task 11.1/11.2/11.2b — build-time only; operator gate: docs/67):
# VITE_API_URL=https://vibenovel-api-staging.moxsenna.workers.dev
#   (AWS HTTPS Task 11.6: VITE_API_URL=https://api-staging.<your-domain>)
#   npm run operator:aws:https:gate -- -Domain <your-domain>
# VITE_SUPABASE_URL=https://<project>.supabase.co
# VITE_SUPABASE_ANON_KEY=<hosted-anon-key>
# VITE_USE_MOCKS=false
# Pages URL: https://vibenovel-web-staging.pages.dev
# Smoke env (operator shell only — never commit):
#   STAGING_SUPABASE_URL, STAGING_SUPABASE_ANON_KEY
#   VIBENOVEL_STAGING_API_URL, VIBENOVEL_STAGING_WEB_URL
# Never put service role or payment keys in web env.

# Mock fallback — default true keeps Sprint 1 UI stable when API is offline
# Set to false + Supabase login for API mode:
#   dashboard, settings, foundation (Task 2.13)
#   intake, concepts, foundation proposals/readiness/lock (Task 3.6)
#   outline cerita generate/edit/approve/lock (Task 4.6); E2E smoke (Task 4.8)
#   write room session/beats/prose/context preview (Task 5.5)
#   summary page generate/delta/approve/proposal review (Task 6.5)
#   publish page load/generate/edit/checklist/mark-exported (Task 7.4)
#   WritePage AI generate button (Task 8.5) - requires API mode + dev:api with AI enabled for real generation
#   WritePage rewrite UI Perbaiki Teks (Task 9.4) - API mode only; mock/fallback shows disabled explanation
#   PublishPage copy AI Perbaiki Copy (Task 9.6) - API mode only; suggestion-first, user applies via PATCH
#   CreditTopupPage (Task 10.4) - API mode + dev:api CREDIT_TOPUP_ENABLED=true for checkout; mock mode shows explanation only
VITE_USE_MOCKS=true