{
  "name": "@vibenovel/api",
  "private": true,
  "version": "0.1.0",
  "description": "VibeNovel API — Hono on Cloudflare Workers (Sprint 2+)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "@vibenovel/shared": "*",
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.94.0"
  }
}